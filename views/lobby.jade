extends ./layout

block content
  .container
    .row
      img(src="../assets/splash2-01.png")
    .logo
      h1 Lane Heroes
      if isHost
        h2 Waiting for player two
      else
        h2 Waiting for host to start
    .content_container
      .info
        p Access code:&nbsp;
          span(id="accessCode") #{accessCode}
        p Welcome&nbsp;
          span(id="playerName") #{playerName}
          | !
      .server_messages
        input(type="hidden", id="isHost" value="#{isHost}")
        div(id="serverMessages")
      if isHost
        .buttons
          a.button(href="/game/#{accessCode}" id="startGame") Start Game

          a.button(href="/", class="back") Leave Game
      else
        .buttons
          a.button(href="/", class="back") Leave Game

  script(src="https://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.4/jquery.min.js")
  script(src="https://cdn.socket.io/socket.io-1.3.7.js")
  script(src="/js/lobby/socket.js")
